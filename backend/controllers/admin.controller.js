import doctorModel from "../models/doctor.model.js";


// add doctor
const addDoctor = async (req, res) => {
  try {
    const {name, email, password, specialization, experience, fees, degree, about, address} = req.body;

    
  } catch (error) {
    
  }
}